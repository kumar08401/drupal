name: Git to K8 deploy

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  checkout:
    runs-on: self-hosted
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        path: main
  docker-login:
    needs: checkout
    runs-on: self-hosted
    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: kumar08401
          password: $Zaq1Xsw2
  docker-build:
    needs: docker-login
    runs-on: self-hosted
    steps:
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: kumar08401/githubactions:2

      
  #build:
    #needs: checkout
    #runs-on: ubuntu-latest
    #steps:
    #- uses: actions/checkout@v2
    #- name: Buildd the Docker image
      #run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
  #login:
    #needs: build
    #runs-on: ubuntu-latest
    #steps:
    #- name: Login to Docker Hub
      #uses: docker/login-action@v3
      #with:
        #username: kumar08401
        #password: $Zaq1Xsw2
  #tag:
    #needs: login
    #runs-on: ubuntu-latest
    #steps:
    #- uses: actions/checkout@v2
    #- name: tag Docker image
      #run: docker push . --file Dockerfile --tag my-image-name:$(date +%s)
      #run: docker tag my-image-name:$(date +%s) yourhubusername/verse_gapminder:my-image-name:$(date +%s)
